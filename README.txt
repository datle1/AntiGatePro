ANTI-GATEPRO version 1.0 (compatiable with Windows OS)
LICENSE BY DATLE

- Prepare environment:
+ Edit path in initGatePro.bat to call GatePro
+ Edit path of Java, path of pop3_client, user, reversed_pass in start.bat
+ Fill IP or node name need open firewall into file ip.list (max is 3 IP)
+ Add "0" param to end of command line to bypass IP limitation

- Run:
start.bat
